export enum AttendanceStatus {
  Present = 'present',
  Absent = 'absent',
  Excused = 'excused',
  Late = 'late',
}

export enum AttendanceSessionType {
  Liturgy = 'liturgy',
  Activity = 'activity',
  General = 'general',
}
